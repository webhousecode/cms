/**
 * RefreshIndicator is now handled natively in WebhouseViewController.swift
 * as a UIView overlay. This React component is kept as a no-op placeholder
 * so existing imports don't break.
 */
export function RefreshIndicator() {
  return null;
}
