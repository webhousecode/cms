# Features

Each feature has its own plan-doc here, named `F<n>-<slug>.md`.
New plan-docs are written by the `feature` skill via the `cardmem_write_plan` MCP tool, which commits via cardmem's GitHub App.

Don't hand-edit this README — it exists to make the folder visible in git. The live, machine-readable feature list is the cardmem board.
