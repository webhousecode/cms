# Features

This project's features live on the [cardmem](https://www.cardmem.com) board.
Each feature has its own plan-doc under `docs/features/F<n>-<slug>.md`.

For the live, machine-readable feature list, query `cardmem_list_cards` via the cardmem MCP server.
